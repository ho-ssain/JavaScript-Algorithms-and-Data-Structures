// 1. Using Recursion:

// function factorialize(num) {
//   if (num === 0 || num === 1) return 1;
//   else return num * factorialize(num - 1);
// }

// factorialize(5);

// 2. Without Recursion:

// function factorialize(num) {
//   let result = 1;
//   for (let i = 2; i <= num; i++) result *= i;
//   return result;
// }

// factorialize(5);

// 3. Optimized Way (Without Recursion, With Memoization):

// function factorialize(num) {
//   let memo = {};

//   const factorial(n){

//   }

//   return factorial(n);
// }

// factorialize(5);
